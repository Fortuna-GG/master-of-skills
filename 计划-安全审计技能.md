# 计划：创建"安全审计"技能（敏感信息 / 注入 / 配置泄露检查）

- 日期：2026-08-12
- 状态：已完成

## 🎯 目标
- 新建一个专注"代码安全审计"的技能，检查：① 敏感信息与个人信息泄露；② SQL 等注入漏洞；③ 配置文件明文敏感信息；④ 其它安全隐患。
- 只出分级中文报告 + 转诊建议，不直接改代码（与 unit-test 相同定位）。

## 📋 需求（用户提出，4 点）
1. 检查项目代码中是否有敏感信息（密钥、口令、token）以及个人信息（身份证、手机号、邮箱等），防泄露。
2. 检查 SQL 等代码是否有注入等漏洞风险（字符串拼接 SQL、格式化拼 SQL、命令注入等）。
3. 检查配置文件（.env、config.*、*.json/yml/toml）中是否有明文敏感信息。
4. 其它有安全隐患的代码（危险函数、硬编码口令、日志打印密钥、不安全的随机数等）。

## 🔍 调研结论（2026-08-12）
- 现有技能对照：
  - unit-test（代码健康体检）：8 维度中的"安全基础"只做浅层（eval/exec/innerHTML、硬编码密钥格式）；无 PII、无 SQL 注入专项、无配置文件明文专项。
  - code-review：审 git 变更（规范+规格），非安全专项。
  - diagnosing-bugs：查疑难 bug，非安全专项。
  - improve-codebase-architecture：架构重构，非安全专项。
- 结论：无完全重复技能，需要新建；新建方式有 4 个候选方案待用户确认。

## 🧭 候选方案（2026-08-12 用户选定方案A）
- 方案A：新建独立技能 /security-audit（Node 机械扫描脚本 + AI 复核 + 分级中文报告），与 unit-test 互补。
- 方案B：扩展 unit-test，把安全检查做深。
- 方案C：新建技能但底层调用专业开源扫描器（gitleaks / semgrep 等），需安装外部工具。
- 方案D：不新建，直接用 unit-test 现有能力。

## ✅ 验收标准（全部达成）
- 技能三件套（SKILL.md / scripts/scan.js / agents/openai.yaml）部署到 .codex/skills/security-audit/，结构校验通过。
- scan.js 样例命中：硬编码密钥、PII（手机号/邮箱/身份证）、SQL 引号拼接/格式化/模板字符串、命令注入、XSS、配置文件明文（含 JSON 键与前缀键名）；normal 文件零误报。
- 技能管家真实项目扫描：正式代码仅 10 条命中、无假阳性爆炸。
- 报告分级（严重/警告）+ 位置 + 转诊建议；中文无乱码。
- unit-test 转诊表补充"安全专项 → /security-audit"。
- 记忆库档案卡 + 总目录 + 经验笔记更新。
- 技能管家重新扫描收录新技能（catalog 142 条），git 提交推送。

## 📝 执行记录
- [2026-08-12] 创建本计划文件；完成现有技能调研（无重复技能）。
- [2026-08-12] 用户选定方案A（新建独立技能 /security-audit）；细化步骤与验收标准。
- [2026-08-12] 创建技能三件套（SKILL.md / scripts/scan.js / agents/openai.yaml）并安装到 .codex/skills/security-audit/。
- [2026-08-12] 样例调试修复 3 处：正则 $ 未转义导致 SQL 模板字符串漏检；CONFIG_RE 不支持 JSON 键引号与前缀键名（DB_PASSWORD 漏检）；命令注入规则不支持引号外拼接、XSS 规则误拼。
- [2026-08-12] 样例验证 22 条命中覆盖五大类，normal 文件零误报；技能管家真实项目扫描无假阳性爆炸。
- [2026-08-12] unit-test 转诊表补充"安全专项 → /security-audit"，协作闭环。
- [2026-08-12] 更新记忆库档案卡与经验笔记；技能管家 catalog 重扫收录新技能（142 条）。
- [2026-08-12] git 提交推送。
